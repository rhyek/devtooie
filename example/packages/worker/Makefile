.PHONY: start

# `start` is the dev process (see run.command in devtooie.config.ts). `go run .` compiles from
# current source and runs, so it's always a clean rebuild — no separate build/clean targets are
# needed for development, and a restart (or rebuild) just re-runs it.
start:
	@go run .
