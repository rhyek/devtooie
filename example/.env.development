NODE_OPTIONS="$NODE_OPTIONS --disable-warning=ExperimentalWarning"

# Read by devtooie.config.ts's `port` callback for the backend, which devtooie then injects
# into the process as PORT.
BACKEND_PORT=3001
